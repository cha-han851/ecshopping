<ul class="news-list">
<% @articles.each do |article|%>
<li><%= article.created_at.strftime('%Y/%m/%d')%>
<%= link_to article_path(article.id), class:"no-under-line" do%>
  <%= article.title %>
  <% end %>
  </li>
<% end%>
</ul>